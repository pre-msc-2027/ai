[pytest]
testpaths = tests_experiments
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
    --tb=short
    --cov=experiments
    --cov-report=term-missing
    --cov-report=html:htmlcov-experiments
asyncio_mode = auto
